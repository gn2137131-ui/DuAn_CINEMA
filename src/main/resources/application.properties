spring.application.name=ticketsystem

# Load local secrets (khÃ´ng commit file application-local.properties)
spring.config.import=optional:classpath:application-local.properties

server.port=${PORT:8080}

# Káº¿t ná»‘i MySQL (Há»— trá»£ cáº£ 2 Ä‘á»‹nh dáº¡ng biáº¿n mÃ´i trÆ°á»ng cá»§a Railway)
spring.datasource.url=jdbc:mysql://${MYSQLHOST:${MYSQL_HOST:localhost}}:${MYSQLPORT:${MYSQL_PORT:3306}}/${MYSQLDATABASE:${MYSQL_DATABASE:cinema_db}}
spring.datasource.username=${MYSQLUSER:${MYSQL_USER:root}}
# Máº­t kháº©u Ä‘Æ°á»£c Ä‘á»‹nh nghÄ©a trong application-local.properties hoáº·c Railway
spring.datasource.password=${MYSQLPASSWORD:${MYSQL_PASSWORD:${DB_PASSWORD:}}}

# Cáº¥u hÃ¬nh Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# cloudinary
# Cáº¥u hÃ¬nh tháº­t náº±m trong application-local.properties
cloudinary.cloud_name=${CLOUDINARY_CLOUD_NAME:}
cloudinary.api_key=${CLOUDINARY_API_KEY:}
cloudinary.api_secret=${CLOUDINARY_API_SECRET:}

# Cho phÃ©p Swagger UI hoáº¡t Ä‘á»™ng
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs

# =====================================================
# ðŸ¦ SePay Payment Configuration
# =====================================================
# API Key tá»« SePay (Webhook Secret) - cáº¥u hÃ¬nh tháº­t náº±m trong application-local.properties
sepay.api.key=${SEPAY_API_KEY:}

# Sá»‘ tÃ i khoáº£n nháº­n tiá»n (VD: MBBank, VietcomBank, etc.)
sepay.api.account-number=${SEPAY_ACCOUNT_NUMBER:}

# TÃªn tÃ i khoáº£n (sáº½ hiá»ƒn thá»‹ trÃªn QR code)
sepay.api.account-name=CINEMA

logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.web.servlet.mvc.method.annotation.RequestResponseBodyMethodProcessor=DEBUG

# =====================================================
# ðŸ“§ Email (Gmail SMTP) Configuration
# =====================================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# Cáº¥u hÃ¬nh tháº­t náº±m trong application-local.properties
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.default-encoding=UTF-8

# =====================================================
# ðŸŒ CORS Configuration
# =====================================================
# Danh sÃ¡ch origin Ä‘Æ°á»£c phÃ©p (cÃ¡ch nhau báº±ng dáº¥u pháº©y). Thay Ä‘á»•i khi deploy production.
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173

# =====================================================
# ðŸ”‘ JWT Configuration
# =====================================================
# Fix #2: Secret khÃ´ng hardcode trong source â€” khai bÃ¡o trong application-local.properties
# hoáº·c biáº¿n mÃ´i trÆ°á»ng JWT_SECRET khi deploy production
jwt.secret=${JWT_SECRET:}
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
